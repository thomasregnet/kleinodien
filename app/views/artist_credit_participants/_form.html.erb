<%= form_with(model: artist_credit_participant) do |form| %>
  <% if artist_credit_participant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist_credit_participant.errors.count, "error") %> prohibited this artist_credit_participant from being saved:</h2>

      <ul>
        <% artist_credit_participant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :artist_credit_id, style: "display: block" %>
    <%= form.text_field :artist_credit_id %>
  </div>

  <div>
    <%= form.label :join_phrase, style: "display: block" %>
    <%= form.text_area :join_phrase %>
  </div>

  <div>
    <%= form.label :participant_id, style: "display: block" %>
    <%= form.text_field :participant_id %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
