<%= form_with(model: artist_credit) do |form| %>
  <% if artist_credit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist_credit.errors.count, "error") %> prohibited this artist_credit from being saved:</h2>

      <ul>
        <% artist_credit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
