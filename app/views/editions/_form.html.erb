<%= form_with(model: edition) do |form| %>
  <% if edition.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(edition.errors.count, "error") %> prohibited this edition from being saved:</h2>

      <ul>
        <% edition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :archetype_id, style: "display: block" %>
    <%= form.text_field :archetype_id %>
  </div>

  <div>
    <%= form.label :editionable_type, style: "display: block" %>
    <%= form.text_field :editionable_type %>
  </div>

  <div>
    <%= form.label :editionable_id, style: "display: block" %>
    <%= form.text_field :editionable_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
